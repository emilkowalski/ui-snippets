import { styled } from 'stitches.config';

const Heading = styled('h1', {
  fontSize: '40px',
  lineHeight: '1.1',
  letterSpacing: '-.89px',
  fontWeight: '$bold',
  display: 'block',
  textAlign: 'center',
  margin: 0
});

export default Heading;
